export default function Loading() {
    return (
        <main className="mt-20 flex justify-center text-white">
            <h1 className="after:animate-ellipsis w-[4em] text-left text-[1.5em] font-bold">
                {"loading"}
            </h1>
        </main>
    );
}
